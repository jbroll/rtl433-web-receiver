# 16 MB, which is what the chip and the board both declare. Arduino's
# default.csv addresses only the first 4 MB, which is why the app read as
# nearly full while using 7% of the flash.
#
# nvs, otadata and app0 keep their conventional offsets because the
# espressif32 platform hardcodes where it writes them rather than reading
# this file: moving app0 leaves it blank and the board boot-loops. Growing
# nvs, which the WiFi provisioning note in the backlog wants, needs that
# solved first.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
app0,       app,  ota_0,    0x10000,  0x400000
app1,       app,  ota_1,    0x410000, 0x400000
spiffs,     data, spiffs,   0x810000, 0x7E0000
coredump,   data, coredump, 0xFF0000, 0x10000
