# Copy to .env and fill in. .env is gitignored and never reaches the build
# output; load_env.py turns each line into a -D build flag.
WIFI_SSID="your-ssid"
WIFI_PASSWORD="your-password"
MDNS_PREFIX="rtl433"
OTA_TOKEN="generate-your-own-32-hex-chars"
