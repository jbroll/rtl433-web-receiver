// receiver/test/fixtures.js is the canonical source (CommonJS, shared with
// the firmware's own test suite); this re-exports it for the ESM specs here.
export * from "../../receiver/test/fixtures.js";
